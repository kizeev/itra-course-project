{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block title_body %}{{ title }}{% endblock %}

{% block body %}
    <ul>
        {% for user in users %}
        <li>{{ user.name }} | {{ user.email }} | {{ user.roles | json_encode }}</li>
        {% endfor %}
    </ul>

{% endblock %}
